---
import { text } from "@src/language/text";
import type { Language } from "@src/language/types";
import styles from "@src/styles/index.module.css";
import "@src/styles/aksel.css";
import { isLocal } from "../../utils/environment";
import Komponent from "@src/components/Komponent.astro";

const language = Astro.currentLocale as Language;
---

{isLocal &&
<meta charset="UTF-8" />}
<div class="pam-min-side-microfrontend">
  <div class={styles.microfrontend}>
    <Komponent heading={text.title[language]} description={text.description[language]} />
  </div>
</div>
<script>
  import { logClickEvent } from "@src/utils/analytics";

  const microfrontend = document.querySelector("#link");
  microfrontend?.addEventListener("click", () => logClickEvent());
</script>
